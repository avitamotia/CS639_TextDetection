"""
Experiment C: compare **one-class** vs **soft-boundary** hypersphere objectives.

Run from repo root (with deps installed, Conda recommended):

    python -m experiments.objective_ablation.run_experiment_c --help
"""
