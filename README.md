# CS639 Text Detection (Group 1)

LLM-generated Text Detection — Pranshu Dewagan, Ernie Dippold, Akshat Jain, Aviaditya Tamotia.

## Install packages into anaconda

## EDA (DeepFake / MAGE dataset)

Run the initial exploratory data analysis on the MAGE (DeepFake) dataset:

```bash
jupyter notebook eda_deepfake.ipynb
```

The notebook loads `yaful/MAGE` from HuggingFace and checks shape, labels, domains (`src`), text length stats, and sample rows.
